/** Default base URL for the Interfaze API (OpenAI-compatible `/v1` surface). */
export const INTERFAZE_BASE_URL = 'https://api.interfaze.ai/v1';
/** Canonical Interfaze model id; anchors the {@link InterfazeChatModelId} union for autocomplete. */
export const INTERFAZE_MODEL = 'interfaze-beta';
